/** @type {import('next').NextConfig} */
const nextConfig = {
	experimental: {
		// staleTimes: {
		// 	dynamic: 0, // default 0
		// 	static: 0, // default 5m.
		// },
	},
};

export default nextConfig;
